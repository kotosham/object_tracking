# ML / open-vocab detector dependencies for object_tracking (edge GPU side).
#
# Install into a venv created WITH access to the system ROS 2 packages so the
# tracker node can import rclpy / cv_bridge:
#
#   python3 -m venv --system-site-packages ~/ot_venv
#   source ~/ot_venv/bin/activate
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#   pip install git+https://github.com/ChaoningZhang/MobileSAM.git
#
# CRITICAL pins (Ubuntu 24.04 / ROS 2 Jazzy, verified working set):
#   numpy<2 and scipy<1.14 are REQUIRED. System ROS packages (cv_bridge,
#   matplotlib) are compiled against numpy 1.26 (noble). numpy 2.x breaks the
#   cv_bridge import (C-ABI), and a numpy-2-era scipy references np.long which
#   numpy 1.26 does not have ("module 'numpy' has no attribute 'long'").
#   Keeping the venv on numpy 1.26.4 + scipy 1.13.1 aligns it with ROS.
#
# torch is NOT pinned here — install it from the CUDA wheel index (see above);
# verified with torch 2.6.0+cu124 on a GTX 1650 (CUDA 12.7 driver).
# mobile_sam is not on PyPI — install from the git URL above (pulls timm).
#
# Verified-working versions (2026-06): numpy 1.26.4, scipy 1.13.1,
# ultralytics 8.4.75, transformers 5.12.1, timm (latest), pillow 10.x.

numpy<2
scipy<1.14
ultralytics
transformers
timm
pillow

# Текстовый энкодер YOLOE. Ставить ОБЯЗАТЕЛЬНО заранее, хотя ultralytics умеет
# доставить его сам: на первом же текстовом промпте (set_classes/get_text_pe)
# он объявляет «requirement not found, attempting AutoUpdate», клонирует этот
# репозиторий и собирает колесо ПРЯМО В РАНТАЙМЕ. Замерено в работающем стеке,
# и последствий три:
#   * ~10 с полной блокировки узла — детектор в это время не отвечает и не шлёт
#     heartbeat, потребители объявляют его мёртвым;
#   * требуется интернет в момент работы — на роботе и edge-боксе его может не
#     быть вообще, и тогда открытый словарь просто не заведётся;
#   * ГЛАВНОЕ: после доустановки ultralytics пишет «Restart runtime for updates
#     to take effect», и это не формальность. Модель в текущем процессе
#     остаётся сломанной — в журнале пошли «segment failed: 'Conv' object has
#     no attribute 'bn'» и «Expected all tensors to be on the same device», а
#     детектор до конца сессии отдавал 0 кандидатов на любых кадрах.
# То есть без этой строки детектор молча слеп до перезапуска стека.
clip @ git+https://github.com/ultralytics/CLIP.git
